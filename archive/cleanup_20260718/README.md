# Cleanup Archive 2026-07-18

This directory contains files moved out of the active project tree during the 2026-07-18 cleanup.

- `experiments/`: TestA and TestB code, documentation, and results.
- `results/`: B5, smoke-test, incomplete Vessel, superseded baseline, and test-only outputs.
- `plots/`: generated comparison, test, and debugging plots.
- `logs/`: historical experiment logs.
- `code/src/`: superseded standalone DYN, B5, and adapted baseline modules.
- `MANIFEST.txt`: relative file paths and byte sizes captured after the move.

The active `results/online_link_prediction/` directory retains only seven complete, non-test runs. Confirmed historical paper results remain under `results/final/`.
